<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yihexinda.dataservice.dao.TLinkDao">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.yihexinda.data.dto.TLinkDto">
        <id column="link_id" property="linkId" />
        <result column="link_length" property="linkLength" />
        <result column="link_dir" property="linkDir" />
        <result column="from_node" property="fromNode" />
        <result column="to_node" property="toNode" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        link_id, link_length, link_dir, from_node, to_node
    </sql>

</mapper>
